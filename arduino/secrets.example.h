// Copy this file to "secrets.h" (same folder as each .ino) and fill in real values.
// secrets.h is in .gitignore so your real WiFi password and device key never get
// committed to the public repo. secrets.example.h (this file) is safe to commit —
// it has no real values in it.

#define WIFI_SSID     "YOUR_WIFI_NAME"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
#define DEVICE_KEY    "YOUR_RFID_DEVICE_KEY"   // must match RfidDeviceKey in appsettings.json
